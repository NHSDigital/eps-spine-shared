# GitHub workflows
id-token: write
echo "oidc-token=${oidc_token}" >> "${GITHUB_OUTPUT}"
echo "api-token=${api_token}" >> "${GITHUB_OUTPUT}"

# Test data
"agentSystemSDS1CodeSystem": *
"cancellationResponseCodeSystem": *

# Misc
CODESYSTEM*
Error base*
CANCEL_SUCCESS_RESPONSE_CODE_SYSTEM*
